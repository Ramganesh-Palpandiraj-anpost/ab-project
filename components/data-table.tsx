"use client";
import { flexRender, getCoreRowModel, getFilteredRowModel, useReactTable, type ColumnDef } from "@tanstack/react-table"; import { Search } from "lucide-react"; import { useState } from "react";
export function DataTable<T>({data,columns,search="Search records..."}:{data:T[];columns:ColumnDef<T>[];search?:string}){const [filter,setFilter]=useState("");const table=useReactTable({data,columns,state:{globalFilter:filter},onGlobalFilterChange:setFilter,getCoreRowModel:getCoreRowModel(),getFilteredRowModel:getFilteredRowModel()});return <div><div className="relative mb-4 max-w-sm"><Search className="absolute left-3 top-3 size-4 text-slate-400"/><input className="input pl-9" placeholder={search} value={filter} onChange={e=>setFilter(e.target.value)}/></div><div className="overflow-x-auto rounded-xl border"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500 dark:bg-slate-800/70">{table.getHeaderGroups().map(g=><tr key={g.id}>{g.headers.map(h=><th className="px-4 py-3" key={h.id}>{flexRender(h.column.columnDef.header,h.getContext())}</th>)}</tr>)}</thead><tbody>{table.getRowModel().rows.map(r=><tr className="border-t hover:bg-slate-50/70 dark:hover:bg-slate-800/40" key={r.id}>{r.getVisibleCells().map(c=><td className="whitespace-nowrap px-4 py-3" key={c.id}>{flexRender(c.column.columnDef.cell,c.getContext())}</td>)}</tr>)}</tbody></table>{!table.getRowModel().rows.length&&<p className="p-8 text-center text-sm text-slate-500">No matching records found.</p>}</div></div>}
